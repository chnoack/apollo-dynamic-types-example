
type Review {
    origin: String
    review_type: String!
    value: String!
    user: String
}

type Book {
    id: ID!
    name: String!
    review: Review
}

type Query {
    books: [Book!]!
    book(id: ID!): Book!
}